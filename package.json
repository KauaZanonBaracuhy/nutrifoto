{
  "name": "nutrifoto",
  "version": "0.1.0",
  "description": "Analise nutricional por foto usando IA (OpenRouter)",
  "type": "module",
  "scripts": {
    "build:web": "node scripts/generate-config.js",
    "build": "node scripts/generate-config.js",
    "serve": "npx serve www",
    "build:android": "echo 'Android pausado temporariamente. Para retomar, restaure a pasta _android_pausado/ para android/ e ajuste capacitor.config.json.' && exit 1",
    "build:apk": "echo 'Android pausado temporariamente. Para retomar, restaure a pasta _android_pausado/ para android/ e ajuste capacitor.config.json.' && exit 1",
    "sync": "echo 'Android pausado. Use build:web para producao.' && exit 1",
    "cap:open": "echo 'Android pausado. Use build:web para producao.' && exit 1"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/camera": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  }
}