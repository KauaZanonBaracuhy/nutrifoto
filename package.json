{
  "name": "nutrifoto",
  "version": "0.1.0",
  "description": "Análise nutricional por foto usando a API do Claude",
  "type": "module",
  "scripts": {
    "generate-config": "node scripts/generate-config.js",
    "build:web": "node scripts/generate-config.js",
    "build:android": "node scripts/generate-config.js && npx cap copy android",
    "build:apk": "node scripts/generate-config.js && npx cap copy android && cd android && gradlew.bat assembleDebug",
    "build": "node scripts/generate-config.js",
    "serve": "npx serve .",
    "sync": "npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/camera": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  }
}
