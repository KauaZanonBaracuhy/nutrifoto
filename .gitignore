node_modules/
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/captures/
android/.idea/
*.iml
.gradle/
local.properties

# Chave de API pessoal (NÃO COMMITAR)
www/js/config.local.js

# Arquivo .env (chaves reais, NÃO commitar)
.env

# Build outputs (APKs, etc.)
builds/

# Netlify CLI (configuração local, não versionar)
.netlify/

# Referência local de Git para Claude Code (NÃO COMMITAR)
GIT_INFO.local.md