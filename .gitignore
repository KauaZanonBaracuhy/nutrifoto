node_modules/
_android_pausado/.gradle/
_android_pausado/build/
_android_pausado/app/build/
_android_pausado/local.properties
_android_pausado/captures/
_android_pausado/.idea/
*.iml
.gradle/
local.properties

# www/ é a pasta de produção e ESTÁ versionada (fonte do deploy Netlify).
# Apenas o config.local.js (gerado a partir do .env) é ignorado.
www/js/config.local.js

# Diretórios de trabalho local (não usados pelo deploy — ver BUILD.md)
shared/
web/

# Arquivo .env (chaves reais, NÃO commitar)
.env

# Build outputs (APKs, etc.)
builds/

# Netlify CLI (configuração local, não versionar)
.netlify/

# Referência local de Git para Claude Code (NÃO COMMITAR)
GIT_INFO.local.md